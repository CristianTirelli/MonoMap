# TODO
# make a list of longest data dependencies, first longest dependency then less long dependency
# make it draw poisson from such list

# make it position first x elements, find solution, then position next x elements, find a solution, and so on

# algoritmi di partitioning -> partitioning dei grafici e poi merging dei sottographi
